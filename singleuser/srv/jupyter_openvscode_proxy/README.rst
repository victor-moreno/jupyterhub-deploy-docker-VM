jupyter openvscode proxy

Adds a proxy service to jupyterlab to open openvscode server

config file:
    ~/.jupyter/services.yaml

    - openvscode:
        new_browser: False  # if True new browser tab is opened
